<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techcolon.lookabook">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LookABook">
        <activity android:name=".FilterActivity"></activity>
        <activity android:name=".SearchActivity" />
        <activity android:name=".SingleBookActivity" />
        <activity android:name=".FirstScreenActivity" />
        <activity
            android:name=".AddBookActivity"
            android:windowSoftInputMode="adjustResize" /> <!-- Lock in Portrait -->
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".SignupActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>